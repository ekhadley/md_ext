# TODO
- sidebar dropdown buttons are rendering under the header instead of to the left of them
- sidebar should be resize-draggable
- remove settings menu from top right; settings should be accessed via the extension button in the chrome header menu
- print menu adds weird formatting (file name at top/bottom of every page); probably need custom PDF export instead of chrome's default 'print page'
